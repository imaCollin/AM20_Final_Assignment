Place the GitLab Handbook Markdown files in this directory.

Expected input:

- Around 50 `.md` files
- Nested folders are supported
- Files should be UTF-8 encoded

After adding the dataset, run:

```bash
python -m src.rag_chatbot.ingest
```
